# Flutter client — build/runtime config (no secrets)
# Use: flutter run --dart-define-from-file=.env.flutter
# Or: dart-define each key in IDE / CI
# See docs/ENV_OWNERSHIP.md

# Optional: Next.js gateway base URL (autocomplete, VIES). If unset, company lookup still uses icoatlas.sk directly.
GATEWAY_BASE_URL=https://bizagent.sk

# Optional: Firebase App Check reCAPTCHA Enterprise site key (public). Web only.
APP_CHECK_WEB_SITE_KEY=
